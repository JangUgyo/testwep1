{
  "name": "workspace-pro",
  "version": "1.0.0",
  "private": true,
  "description": "WorkSpace Pro — 사내 통합 포털 (모듈 소스 → 단일 HTML 빌드)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node tests/e2e.cjs",
    "ci": "npm run test"
  },
  "devDependencies": {
    "jsdom": "29.1.1"
  }
}
